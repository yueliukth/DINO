{
"ckpt_params": { "output_dir": "/storage/yue/dino_models/test/",
                 "restore_epoch": 0,
               },
"dataset_params": { "dataset_name": "ImageNet", # "ImageNet", "FashionMNIST", "IMAGENETTE"
                    "data_folder": "/storage/yue/data/",
                  },
"dataloader_params": {"trainloader":{"batch_size": 6,
                                     "num_workers": 2,
                                     "pin_memory": True,
                                     "drop_last": True},
                      "valloader": { "batch_size": 6,
                                     "num_workers": 2,
                                     "pin_memory": True,
                                     "drop_last": True}
                     },
"augmentation_params": {"global_crops_scale": [0.4, 1.],
                        "local_crops_scale": [0.05, 0.4],
                        "local_crops_number": 8,
                        "global_size": 224,
                        "local_size": 96,
                       },
"model_params": {"backbone_option":  "vit_tiny", # 'vit_tiny', 'vit_small', 'vit_base', 'resnet50', 'deit_tiny', 'deit_small'
                 "patch_size": 16,
                 "drop_path_rate": 0.1,
                 "out_dim": 65536, # "Dimensionality of the DINO head output. For complex and large datasets large values (like 65k) work well."
                 "use_bn_in_head": False, # "Whether to use batch normalizations in projection head (Default: False)"
                 "norm_last_layer": False, # "Whether or not to weight normalize the last layer of the DINO head.
                 # Not normalizing leads to better performance but can make the training unstable.
                 # In our experiments, we typically set this paramater to False with vit_small and True with vit_base."
                },
"training_params": {"num_epochs":100,
                    "warmup_teacher_temp": 0.04,
                    "teacher_temp": 0.04,
                    "warmup_teacher_temp_epochs": 0,
                    "student_temp": 0.1,
                    "center_momentum": 0.9,
                    "optimizer": {"name": "sgd",
                                  "sgd": { "lr": 0, "momentum": 0.9}},
                    "lr":{"base_lr": 0.0005,
                          "final_lr": 0.000001,
                          "warmup_epochs": 10,
                          "start_warmup_lr": 0},
                    "wd":{"base_wd": 0.04,
                          "final_wd": 0.4},
                    "momentum": {"base_momentum_teacher": 0.996,
                                 "final_momentum_teacher": 1},




                    },
"system_params": {"num_gpus": 2,
                  "gpu_ids": "0,1",
                  "random_seed": 6111,
                  "use_fp16": True},
                 }